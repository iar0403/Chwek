import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wootak.chweck',
  appName: '쳌!(Chweck)',
  webDir: 'out' // Next.js output: 'export'는 out 폴더에 빌드 결과물을 생성합니다
};

export default config;
